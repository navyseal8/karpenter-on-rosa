# ============================================================================
# PodDisruptionBudget — ensures at least 1 pod stays Running during
# voluntary disruptions (node drain, cluster upgrade, consolidation)
# ============================================================================
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: burst-workload-cas-pdb
  namespace: scaling-poc
  labels:
    app.kubernetes.io/name: burst-workload
    app.kubernetes.io/instance: cas-baseline
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: burst-workload
      app.kubernetes.io/instance: cas-baseline
